# Copy to .env.local and adjust. Used for canonical links, sitemap and Open Graph.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Demo only: shows a control on the payment step to force a failed payment. Set to false for a clean demo.
NEXT_PUBLIC_SHOW_MOCK_CONTROLS=true
